<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Document</title>
     <link rel="stylesheet" href="/stylesheet/style.css">
     <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
     <!-- <h1>Hey! Jitin K Gupta 👌</h1> -->
     <div class="main w-full  min-h-screen bg-zinc-900">
          <div class="form p-10 container mx-auto text-white">
               <form action="/create" method="post">
                    <input type="text" class="block w-full rounded-lg px-3 py-2 bg-zinc-800" name="title" id="" placeholder="Title goes here.." required>
                    <textarea class="mt-2 block w-full px-3 py-2 bg-zinc-800 rounded-lg resize-none" name="details" id="" placeholder="Write your task details here.." required></textarea>
                    <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Add Task">
               </form>
          </div>
          
          <div class="tasks flex gap-3 flex-wrap p-10">
               <% if(files.length > 0){ %>
                    <% files.forEach(function(val) { %>
                         <div class="task border w-72 px-3 py-4 rounded-md bg-zinc-800">
                              <h1 class="text-white text-xl tracking-tighter"><%= val.split('.txt').join('')  %></h1>
                              <div class="flex w-full justify-between items-center mt-3">
                                   <a class="text-blue-500 inline-block" href="/file/<%= val  %>">Read more.</a>
                                   <a href="/edit/<%= val %>" class="text-blue-500 inline-block">edit</a>
                              </div>
                         </div>
                    <% }) %>
               <% } else { %>
                    <div class="bg-zinc-800 py-4 px-3 w-72">
                         <h3 class="text-yellow-400">⛔ No tasks yet.</h3>
                    </div>
               <% } %>
          </div>
     </div>
     
</body>
<script src="/javascript/script.js"></script>
</html>